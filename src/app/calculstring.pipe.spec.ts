import { CalculstringPipe } from './calculstring.pipe';

describe('CalculstringPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculstringPipe();
    expect(pipe).toBeTruthy();
  });
});
